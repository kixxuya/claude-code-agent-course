# 가상환경(.venv, Python 3.14)에 실제 설치된 버전으로 고정 (pip freeze 기준, 2026-09-23)
# Playwright는 설치 후 브라우저를 따로 받아야 한다: python -m playwright install chromium

# 수집
requests==2.34.2
beautifulsoup4==4.15.0
playwright==1.63.0

# 전처리 / 분석
pandas==3.0.6

# 설정 / Gemini
python-dotenv==1.2.3
google-genai==2.25.0

# 로컬 노트북 실행용 (main.py 실행에는 필요 없음)
jupyter==1.1.1
