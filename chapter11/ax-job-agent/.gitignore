# 가상환경
.venv/

# 민감정보 (API 키 등) - .env.example만 커밋한다
.env

# Python 캐시
__pycache__/
*.pyc

# Jupyter 자동 저장
.ipynb_checkpoints/

# 주의: data/는 넣지 않는다.
# data/processed/jobs_history.csv는 파이프라인 상태 파일이라 git으로 추적한다
# (주 1회 자동 실행 때 이전 기록이 필요함).
